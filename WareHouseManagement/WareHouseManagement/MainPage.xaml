<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:WareHouseManagement"
             x:Class="WareHouseManagement.MainPage"  
             BackgroundColor="#D1CFCF"
             Title="MANAGEMENT"  >

    <ContentPage.ToolbarItems>
        <ToolbarItem Icon="power_1.PNG" Clicked="OnLogoutButtonClicked" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" Padding="10" VerticalOptions="Fill" >
          
                <StackLayout  >

                <Grid VerticalOptions="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />

                    </Grid.ColumnDefinitions>
                    <Button x:Name="btn_StockIn" Grid.Row="0" BorderRadius="5" Grid.Column="0" Image="warehouse" BackgroundColor="White" ContentLayout="Top,30" Text="Stock In" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" Clicked="btn_StockIn_Clicked">

                    </Button>
                    <Button x:Name="btn_PalletMaintainance"  BorderRadius="5"  Grid.Row="0" Grid.Column="1" BackgroundColor="White" ContentLayout="Top,10"  Text="Pallet MainTainance" Image="storage" VerticalOptions="FillAndExpand"  HorizontalOptions="FillAndExpand" Clicked="btn_PalletMaintainance_Clicked">

                    </Button>
                    <Button x:Name="btn_ClrPalletTag"  BorderRadius="5" Grid.Row="1" Grid.Column="0" BackgroundColor="White" ContentLayout="Top,20" Image="price_tag" Text="Clear Pallet Tag" VerticalOptions="FillAndExpand"  HorizontalOptions="FillAndExpand" Clicked="btn_ClrPalletTag_Clicked">

                    </Button>
                    <Button x:Name="btn_StockOut"  BorderRadius="5"  Grid.Row="1" Grid.Column="1" BackgroundColor="White" ContentLayout="Top,30"  Image="delivery_truck" Text="Stock Out" VerticalOptions="FillAndExpand"  HorizontalOptions="FillAndExpand" Clicked="btn_StockOut_Clicked">

                    </Button>
                    <Button  Image="checklist.png"   Grid.Row="2"  BorderRadius="5" Grid.Column="0" BackgroundColor="White"  ContentLayout="Top,30" Text="Pickup List " HorizontalOptions="FillAndExpand" >

                    </Button>
                    <Button x:Name="btn_ReaderList" Text="ReaderList" Image="smartphone.png" ContentLayout="Top,30" Grid.Row="2"  BorderRadius="5" BackgroundColor="White" Grid.Column="1" Clicked="btn_ReaderList_Clicked" >

                    </Button>
                </Grid>






            </StackLayout>
        </StackLayout>

    </ContentPage.Content>
</ContentPage>
